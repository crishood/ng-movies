import { FavDirective } from './fav.directive';

describe('FavDirective', () => {
  it('should create an instance', () => {
    const directive = new FavDirective();
    expect(directive).toBeTruthy();
  });
});
